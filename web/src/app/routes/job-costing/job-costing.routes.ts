import { Routes } from '@angular/router';
import { JobCostingListComponent } from './job-costing-list/job-costing-list';

export const routes: Routes = [
  { path: '', component: JobCostingListComponent },
];
