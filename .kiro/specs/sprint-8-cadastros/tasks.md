# Tasks — Sprint 8: Cadastros Auxiliares

## Feedback loops
```bash
./mvnw compile -s ~/.m2/settings-local.xml
./mvnw test -s ~/.m2/settings-local.xml
```

## Task list

### Task 1: Migration V12 — tabelas tipo_unidade, especie_insumo, tipo_usuario
- **Commit:** `migration: add tipo_unidade, especie_insumo, tipo_usuario tables`

### Task 2: CRUD Tipo de Unidade
- **Commit:** `feat(tipo-unidade): full CRUD`

### Task 3: CRUD Espécie de Insumo (dinâmico, substituir enum)
- **Commit:** `feat(especie-insumo): full CRUD replacing fixed enum`

### Task 4: CRUD Tipo de Usuário
- **Commit:** `feat(tipo-usuario): full CRUD`
